Text version:
a) limit to 00,15,30,45 minutes
b) add view all work-days function
c) add multiple work-day entry

App version:
a) debug: issue of redrawing issues and key binding issues after closing a messagebox
tried: add root as a parameter and do root.update_idletasks(). -> failed
possible solution: check with chatgpt
if not works: dont use messagebox -> use tk.Label for logs and errors
